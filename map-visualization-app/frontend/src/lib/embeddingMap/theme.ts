/*
 * For licensing see accompanying LICENSE file.
 * Copyright (C) 2025 Apple Inc. All Rights Reserved.
 */

import { type Theme } from 'embedding-atlas';

export const customTheme: Theme = {
	fontFamily: 'system-ui,sans-serif',
	clusterLabelColor: '#000', // default black
	clusterLabelOutlineColor: 'rgba(255,255,255,0.8)', // default white 0.8 opacity
	clusterLabelOpacity: 1, // default 0.8 opacity
	statusBar: false
};

export const defaultPointColor = ['#007AFF'];
